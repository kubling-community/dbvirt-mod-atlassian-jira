/**
 * Jira Software Cloud API
 * Jira Software Cloud REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BuildCommitReference model module.
 * @module model/BuildCommitReference
 * @version 1001.0.0
 */
class BuildCommitReference {
    /**
     * Constructs a new <code>BuildCommitReference</code>.
     * Details about the commit the build was run against. 
     * @alias module:model/BuildCommitReference
     * @param id {String} The ID of the commit. E.g. for a Git repository this would be the SHA1 hash. 
     * @param repositoryUri {String} An identifier for the repository containing the commit.  In most cases this should be the URL of the repository in the SCM provider.  For cases where the build was executed against a local repository etc. this should be some identifier that is unique to that repository. 
     */
    constructor(id, repositoryUri) { 
        
        BuildCommitReference.initialize(this, id, repositoryUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, repositoryUri) { 
        obj['id'] = id;
        obj['repositoryUri'] = repositoryUri;
    }

    /**
     * Constructs a <code>BuildCommitReference</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BuildCommitReference} obj Optional instance to populate.
     * @return {module:model/BuildCommitReference} The populated <code>BuildCommitReference</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BuildCommitReference();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('repositoryUri')) {
                obj['repositoryUri'] = ApiClient.convertToType(data['repositoryUri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BuildCommitReference</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BuildCommitReference</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BuildCommitReference.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['repositoryUri'] && !(typeof data['repositoryUri'] === 'string' || data['repositoryUri'] instanceof String)) {
            throw new Error("Expected the field `repositoryUri` to be a primitive type in the JSON string but got " + data['repositoryUri']);
        }

        return true;
    }


}

BuildCommitReference.RequiredProperties = ["id", "repositoryUri"];

/**
 * The ID of the commit. E.g. for a Git repository this would be the SHA1 hash. 
 * @member {String} id
 */
BuildCommitReference.prototype['id'] = undefined;

/**
 * An identifier for the repository containing the commit.  In most cases this should be the URL of the repository in the SCM provider.  For cases where the build was executed against a local repository etc. this should be some identifier that is unique to that repository. 
 * @member {String} repositoryUri
 */
BuildCommitReference.prototype['repositoryUri'] = undefined;






export default BuildCommitReference;

