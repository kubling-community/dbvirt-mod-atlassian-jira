/**
 * Jira Software Cloud API
 * Jira Software Cloud REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnvironmentDetails from './EnvironmentDetails';
import FeatureFlagStatus from './FeatureFlagStatus';

/**
 * The FeatureFlagDetails model module.
 * @module model/FeatureFlagDetails
 * @version 1001.0.0
 */
class FeatureFlagDetails {
    /**
     * Constructs a new <code>FeatureFlagDetails</code>.
     * Details of a Feature Flag for a single environment.
     * @alias module:model/FeatureFlagDetails
     * @param url {String} A URL users can use to link to this Feature Flag, in this environment. 
     * @param lastUpdated {Date} The last-updated timestamp for this Feature Flag, in this environment.  Expected format is an RFC3339 formatted string. 
     * @param environment {module:model/EnvironmentDetails} 
     * @param status {module:model/FeatureFlagStatus} 
     */
    constructor(url, lastUpdated, environment, status) { 
        
        FeatureFlagDetails.initialize(this, url, lastUpdated, environment, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, url, lastUpdated, environment, status) { 
        obj['url'] = url;
        obj['lastUpdated'] = lastUpdated;
        obj['environment'] = environment;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>FeatureFlagDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FeatureFlagDetails} obj Optional instance to populate.
     * @return {module:model/FeatureFlagDetails} The populated <code>FeatureFlagDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FeatureFlagDetails();

            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('lastUpdated')) {
                obj['lastUpdated'] = ApiClient.convertToType(data['lastUpdated'], 'Date');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = EnvironmentDetails.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = FeatureFlagStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FeatureFlagDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FeatureFlagDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FeatureFlagDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // validate the optional field `environment`
        if (data['environment']) { // data not null
          EnvironmentDetails.validateJSON(data['environment']);
        }
        // validate the optional field `status`
        if (data['status']) { // data not null
          FeatureFlagStatus.validateJSON(data['status']);
        }

        return true;
    }


}

FeatureFlagDetails.RequiredProperties = ["url", "lastUpdated", "environment", "status"];

/**
 * A URL users can use to link to this Feature Flag, in this environment. 
 * @member {String} url
 */
FeatureFlagDetails.prototype['url'] = undefined;

/**
 * The last-updated timestamp for this Feature Flag, in this environment.  Expected format is an RFC3339 formatted string. 
 * @member {Date} lastUpdated
 */
FeatureFlagDetails.prototype['lastUpdated'] = undefined;

/**
 * @member {module:model/EnvironmentDetails} environment
 */
FeatureFlagDetails.prototype['environment'] = undefined;

/**
 * @member {module:model/FeatureFlagStatus} status
 */
FeatureFlagDetails.prototype['status'] = undefined;






export default FeatureFlagDetails;

