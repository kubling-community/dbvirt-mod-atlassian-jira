/**
 * The Jira Cloud platform REST API
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT-122db9bd6b089919e6465ce52fdce319d5a9dfb4
 * Contact: ecosystem@atlassian.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PriorityMapping from './PriorityMapping';

/**
 * The PrioritySchemeChangesWithMappings model module.
 * @module model/PrioritySchemeChangesWithMappings
 * @version 1001.0.0-SNAPSHOT-122db9bd6b089919e6465ce52fdce319d5a9dfb4
 */
class PrioritySchemeChangesWithMappings {
    /**
     * Constructs a new <code>PrioritySchemeChangesWithMappings</code>.
     * @alias module:model/PrioritySchemeChangesWithMappings
     * @param ids {Array.<Number>} Affected entity ids.
     */
    constructor(ids) { 
        
        PrioritySchemeChangesWithMappings.initialize(this, ids);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ids) { 
        obj['ids'] = ids;
    }

    /**
     * Constructs a <code>PrioritySchemeChangesWithMappings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrioritySchemeChangesWithMappings} obj Optional instance to populate.
     * @return {module:model/PrioritySchemeChangesWithMappings} The populated <code>PrioritySchemeChangesWithMappings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrioritySchemeChangesWithMappings();

            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], ['Number']);
            }
            if (data.hasOwnProperty('mappings')) {
                obj['mappings'] = ApiClient.convertToType(data['mappings'], [PriorityMapping]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrioritySchemeChangesWithMappings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrioritySchemeChangesWithMappings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrioritySchemeChangesWithMappings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['ids'])) {
            throw new Error("Expected the field `ids` to be an array in the JSON data but got " + data['ids']);
        }
        if (data['mappings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['mappings'])) {
                throw new Error("Expected the field `mappings` to be an array in the JSON data but got " + data['mappings']);
            }
            // validate the optional field `mappings` (array)
            for (const item of data['mappings']) {
                PriorityMapping.validateJSON(item);
            };
        }

        return true;
    }


}

PrioritySchemeChangesWithMappings.RequiredProperties = ["ids"];

/**
 * Affected entity ids.
 * @member {Array.<Number>} ids
 */
PrioritySchemeChangesWithMappings.prototype['ids'] = undefined;

/**
 * Instructions to migrate issues.
 * @member {Array.<module:model/PriorityMapping>} mappings
 */
PrioritySchemeChangesWithMappings.prototype['mappings'] = undefined;






export default PrioritySchemeChangesWithMappings;

